/* 
global createCanvas, stroke, triangle, noFill, windowWidth, windowHeight, colorMode, HSL, textSize, text, random, background, fill, ellipse, mouseX, mouseY, collideCircleCircle, width, height, color, abs, noStroke, sqrt, PI
*/
// List all p5 methods you plan to use in the global so that Glitch can recognize them.


function setup() {
  createCanvas(600, 500);
  noStroke();
}

function draw() {
  background(220)
 
  
  fill(80,0,0)
  rect()
  fill(80,0,0)
  rect()
  fill()
  rect()
  fill()
  rect()
  fill()
  rect()
  fill()
  rect()
}

